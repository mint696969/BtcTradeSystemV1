## Btc Ts-ライブ引継ぎ（固定）

※このキャンバスは引継ぎの内容以外書き込みを禁ずる
　大切な内容につきその他の目的で使用せず上書きは禁止です

## 目的

-日々の作業・課題・決定・次アクションを \*\*1 か所\*\に集約し、チャットをまたいだ瞬時の再開を可能にする。

## 記入フォーマット（必須）

```
## <YYYY-MM-DD <短い見出し
  - 作業メモ
    ...

  - 完了タスク
    ...

  - 次の候補タスク
    A) ...

    B) ...

  - 参照: PR/コミット/スクショ/ログ へのリンク or 要約
```

- 作業報告は末尾に追記していくこと。
- 無駄な改行は避け無駄に長くしない事。
- “意味のある粒度”で書く（誰でも追従できるように）。
- 決定事項は `docs/` の該当ファイル（計画/ADR 等）へ\*\*要約のみ\*\*反映。

---

##### 以下直近の作業報告

---

Btc Ts V1 → NEXT 移行 最終報告（2026-1-10 / JST）
1. 本チャットで実施した作業概要（事実ベース）
1.1 Git バックアップ・復元システム（NEXT 正準）の完成

Git バックアップ・復元システム仕様書（NEXT 正準） を策定・確定

設計方針を以下に固定

自動ロールバック禁止（必ず人の判断）

差分成果物（diff / metadata 等）を正準にしない

正準バックアップは git bundle

正準保存先を E:\btc_ts\backup に固定

RP（Restore Point）運用を タグ方式のみ に一本化

GPT 迷子防止のため、外部ルート（E:\btc_ts）の扱いを仕様として明文化

1.2 handoff（CTX zip）生成系の整理と完成

make_handoff.ps1 / gen_summary.py を整理

不要な成果物を排除し、

「次チャットに必要な情報だけ」を確実に梱包する構成に調整

SUMMARY.md を「薄くする」のではなく、

途中でチャットが切れても再開できる密度を持つ構成へ方針確定

1.3 repo 構造抽出系の是正

make_repo_map_extract.py において

旧V1由来パスが混入する根本原因を特定

header（# path:）の誤記を是正

外部ルート除外・整理の思想を NEXT 正準に合わせて統一

旧仕様書・旧 Docs が repo 構造に混入する問題を解消

1.4 旧PC（V1環境）の完全整理

NEXT を旧PCリポジトリから完全除去

V1 を「参照専用・起動専用」として安全に復元

Collector 常駐なし

ダッシュボード単独起動を確認

旧PC上の Git 管理を完全に解除

リポ直下だけでなく

C:\Users\mint777 配下に存在していた .git をすべて削除

結果：

旧PCでは Git が一切作用しない

NEXT 開発（新PC）への干渉リスクなし

2. 現在の状態（最終到達点）
V1（旧PC）

Git：完全に無効

役割：

過去UI・挙動の確認

参考実行のみ

開発継続：なし

NEXT（新PC）

Git 管理：正準状態

バックアップ／復元：設計・仕様ともに完成

handoff（CTX zip）：安定生成可能

次フェーズに進める準備は すべて完了

3. NEXT プロジェクトで次にやるべき優先タスク（推奨）
優先度 A（すぐ着手）

NEXT のコア開発再開

Collector / Health / UI の本格実装

仕様書ベースで迷いなく進められる状態

Git 運用は「触らない前提」で淡々と

RP は節目のみ

bundle は大きな区切りでのみ作成

優先度 B（安定後）

git_full_backup.ps1 の実装（仕様書通り）

handoff 生成を定期運用に組み込むか検討

やらなくてよいこと

V1 側の改修

旧PCでの Git 復活

diff / metadata 系の成果物再導入

4. 気づいたこと・注意点（重要）

「Gitがある＝安全」ではない

むしろ今回のように、
無意識の .git 混入が最大の事故要因になり得る

あなたの

設計思想

仕様書主導

手動レビュー前提
は GPTと非常に相性が良い

今回ここまでスムーズに到達できた理由は

「途中で妥協しなかったこと」

「その場しのぎを一切しなかったこと」
に尽きる

5. 引き継ぎメッセージ（次GPT向け）

V1 は 完全終了

旧PCは 参照専用

NEXT は 設計・運用・バックアップが確定した状態

これ以上「整理フェーズ」に戻る必要はない
→ 開発に集中してよい